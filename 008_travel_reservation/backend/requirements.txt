# Web framework / ASGI server
fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.4

# DB (SQLAlchemy + PyMySQL driver to connect to MySQL)
SQLAlchemy==2.0.40
PyMySQL==1.1.1
python-dotenv==1.1.0

# Data handling (used for DB query results and CSV migration script)
pandas==2.2.3

# Password hash verification
bcrypt==5.0.0

# Testing
pytest==8.3.5
