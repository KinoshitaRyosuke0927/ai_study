{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "acrLoginServer": { "value": "acrplayrole.azurecr.io" },
    "acrUsername": { "value": "<az acr credential show -n acrplayrole で取得したusername>" },
    "acrPassword": { "value": "<az acr credential show -n acrplayrole で取得したpassword>" },
    "imageName": { "value": "acrplayrole.azurecr.io/play-role:latest" },
    "azureOpenAiEndpoint": { "value": "<ワークスペースルートの .env の AZURE_OPENAI_ENDPOINT>" },
    "azureOpenAiKey": { "value": "<ワークスペースルートの .env の AZURE_OPENAI_KEY>" }
  }
}
