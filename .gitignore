# 環境変数ファイル（シークレットが含まれるため Git 管理対象外）
.env
.env.*
!.env.example

# Azure Functions ローカル設定（シークレットが含まれるため Git 管理対象外）
local.settings.json

# ARMデプロイパラメータ（シークレットが含まれるため Git 管理対象外、*.example のみ管理）
**/infra/*.parameters.json

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
.build/
build/
.venv/
venv/

# Node
node_modules/

# アップロード・出力ディレクトリ
*/uploads/
*/outputs/

# 002_graph_gui 生成データ
002_graph_gui/data/flowcharts/
002_graph_gui/data/graph.json

# 004_play_role データ・設定
004_play_role/data/
004_play_role/reviewers.csv

# シークレットが含まれるファイル
docs/Qiita/appendix/crs_extend_01/project_codes.json
011_chat_linkage/mattermost_key.txt

# Kaggle 提出ファイル（大容量・コードではないため管理不要）
**/submission_*.csv

# 学習済みモデルファイル（大容量・コードではないため管理不要）
**/*.pt
**/*.h5
**/*.pkl
**/*.joblib

# 011_chat_linkage 投稿絞り込みモデルの重み（大容量。app/model/train_model.py で再生成可能）
011_chat_linkage/app/model/reminder_classifier/

# CatBoost 学習ログ（自動生成される一時ファイル）
catboost_info/
