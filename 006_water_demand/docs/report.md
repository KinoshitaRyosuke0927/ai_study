# 水の注文量予測モデル 学習記録

---

## STEP1 テーマ決定 + 学習計画作成

### テーマ候補

1. ウォーターサーバーの水の注文量予測モデル
2. 旅行プラン提案AI
3. ボードゲームAI

### 選んだテーマ

1. ウォーターサーバーの水の注文量予測モデル

### 学習計画

**学習の目標**

- 機械学習（回帰モデル）の基本的な流れ（データ収集 → 前処理 → 特徴量エンジニアリング → モデル学習 → 評価 → 予測）を一通り実践できる
- 実業務データ（水の注文量・出社状況・気象）を組み合わせて実用的な予測スクリプトを作成できる

**参考にする資料（Webサイトや書籍）**

- scikit-learn 公式ドキュメント（Ridge, LeaveOneOut, cross_val_score）
- Meteostat API 公式ドキュメント（dev.meteostat.net）

**週ごとのざっくりスケジュール**

| 週 | 内容 | 想定時間 |
|---|---|---|
| 第1週 | STEP1 : テーマ決定 + 学習計画作成 | 1.0 h |
| 第2週 | STEP2 : 技術調査 | 1.0 h |
| 第3週 | STEP3, 4 : 作成するもの決定 + 開発環境決定 | 1.5 h |
| 第4週 | STEP5 : 気象データ取得API実装 + データ整備 | 3.0 h |
| 第5週 | STEP5 : 特徴量エンジニアリング + Ridge回帰実装 | 2.0 h |
| 第6週 | STEP5 : LOO-CVによるハイパーパラメータ選定 + 翌月予測実装 | 1.0 h |
| 第7週 | STEP5 : データ登録機能実装 | 2.0 h |
| 第8週 | STEP6, 7 : 学習内容まとめ + デモ実施 | 1.5 h |
| | **合計** | **13.0 h** |

**プロトタイプの完成イメージ（一言で説明）**

ブラウザから注文量実績・出社カレンダーを登録・編集し、気象データを最新化した上で翌月の推奨注文量（20L単位）を予測・確認できるWebアプリ

---

## STEP2 技術調査

### 技術概要

月次の水の注文量実績・JBD営業日カレンダー・社員別出社カレンダー・気象データを組み合わせて月ごとの特徴量を作成し、Ridge回帰で翌月の注文量を予測する。

外部APIとして Meteostat API（RapidAPI 経由）を使って事務所（AIC）の日次気象データを自動取得する。
LOO-CV（Leave-One-Out Cross Validation）でデータ件数が少ない状況でも汎化性能を適切に評価する。

### どのような課題を解決できるか

| 課題 | 解決内容 |
|---|---|
| 注文量が感覚頼りになっている | 過去データと回帰モデルで定量的な推奨注文量を算出する |
| 過去の注文量が確認できない | 実績のデータを登録/確認する機能を作成する |
| 月の途中で在庫が不足する | 実績のデータを登録する |

### 代表的なAPI・ツール・ライブラリ

| 名称 | 概要 | 特徴 |
|---|---|---|
| scikit-learn（Ridge） | Pythonの機械学習ライブラリ | 正則化付き線形回帰・LOO-CV実装が容易 |
| scikit-learn（LeaveOneOut） | LOO-CV実装 | データ件数が少ない場合の汎化評価に有効 |
| Meteostat API | 地点指定で気象データを取得できるAPI | 緯度経度で日次気象データを取得可能・月500回まで無料 |
| pandas | データ処理ライブラリ | 複数CSVの結合・期間集計・型変換が容易 |
| numpy | 数値計算ライブラリ | 切り上げ計算（np.ceil）等に使用 |

### 調査して感じた疑問点・気になる点

- LOO-CVはデータ件数が少ない（10件程度）場合に特に有効とされているが、月次データで十分機能するか
- 前年同月の気温を代替値として使う場合、年ごとの気候変動で誤差がどの程度発生するか
- 翌月の出社状況が未確定な場合、どのデータを予測の入力として使用すべきか
- 湿度が水の消費量に与える影響は実際に大きいか（現在のAPIでは取得不可）

---

## STEP3 作成するもの決定

### 作成するプロトタイプ案

1. データ登録・予測・履歴確認を一画面でできるWebアプリ
2. 過去データの傾向をグラフで可視化するノートブック
3. 複数の回帰モデル（Ridge・Lasso・Random Forest）を比較するベンチマークスクリプト

### 選んだプロトタイプ

1. データ登録・予測・履歴確認を一画面でできるWebアプリ

### 作成計画

**目的（何を確かめたいか）**

月次の業務データ（注文量・出社状況・気象・営業日数）を組み合わせてRidge回帰モデルを構築し、翌月の水の注文量を実用精度で予測できるかを検証する。
また、CLIスクリプトで確認したモデルをWebアプリとして整備し、実際の業務運用に近い形で使えるかを確認する。

**成功基準の定義**

- 実際に消費された水の量が、予測値を下回らないこと
- 残量が20L以内になっていること
- 予測に必要なデータが揃っていれば、推奨注文量（20L単位）が自動で出力される
- ブラウザから注文量実績・出社カレンダーを登録・編集できる
- 気象データ取得APIが正常動作し、指定期間のデータをCSVに保存できる

---

## STEP4 開発環境決定

### 使用するライブラリやAPI、モデルの比較

| 項目 | Ridge回帰 | Lasso回帰 | Random Forest |
|---|---|---|---|
| データ件数が少ない場合 | ◎ 安定 | ○ 係数がゼロになりすぎる場合あり | △ 過学習しやすい |
| 説明性 | ◎ 係数から解釈可能 | ◎ スパース性あり | △ ブラックボックス |
| ハイパーパラメータ | ◎ alphaのみ | ◎ alphaのみ | △ 複数（木の深さ等） |
| 実装コスト | ◎ 低い | ◎ 低い | ○ やや高い |

| 項目 | Meteostat API | 気象庁API | Open-Meteo |
|---|---|---|---|
| 緯度経度指定 | ◎ 可能 | △ 地点コード指定 | ◎ 可能 |
| 無料枠 | ○ 500回/月 | ◎ 無制限 | ◎ 無制限 |
| 日本語ドキュメント | △ 英語のみ | ◎ 日本語 | △ 英語のみ |
| 取得できる項目 | ○ 気温・降水量等（湿度なし） | ○ 気温・湿度等 | ◎ 気温・湿度等 |

### 選んだ環境

- **回帰モデル**: Ridge回帰（scikit-learn）
  - データ件数が少ない月次データでも安定して機能し、係数から特徴量の影響を解釈しやすい
- **ハイパーパラメータ選定**: LOO-CV（LeaveOneOut + cross_val_score）
  - データが少ない場合に汎化性能を最大限活用できる交差検証手法
- **気象データ取得**: Meteostat API（RapidAPI 経由）
  - 緯度経度で事務所（AIC）の日次気象データを取得可能・無料枠で十分
- **データ処理**: pandas + numpy
  - 複数CSV結合・月次集計・欠損補完の実装コストが低い
- **Webフレームワーク**: FastAPI + Jinja2 + JavaScript
  - Pythonで統一・非同期処理対応・テンプレートエンジンでHTMLをシンプルに記述できる
- **データ永続化**: CSVファイル（DBは使用しない）
  - PoC段階での実装コスト最小化・ファイル単体で内容を確認しやすい

---

## STEP6 学習内容まとめ

### できたこと

**モデル・データ処理**

- 推奨注文量を20L単位に切り上げて出力し、実業務の注文単位に合わせた結果を得られた
- 翌月の営業日・出社予定・気温（前年同月代替）が揃っていれば翌月の推奨注文量を自動算出できた
- Meteostat API（RapidAPI 経由）を使って、AIC所在地（緯度・経度指定）の日次気象データを自動取得し、CSVに保存できた
- Ridge回帰 + LOO-CVで正則化パラメータ（alpha）を候補6種類から自動選定できた

**Webアプリ**

- FastAPI + Jinja2 を使って4画面構成のWebアプリを実装できた
  - メニュー画面（`/`）：各機能へのナビゲーション
  - 水の注文量実績画面（`/water_demand`）：月別の注文量・残量を表形式で表示し、セル直接編集 + 保存ができる
  - AIC出社カレンダー画面（`/work_day`）：社員を切り替えて月別の出社状況を確認・編集できる
  - 注文量予測画面（`/predict`）：使用データの参照期間・更新日時を確認し、ボタン1つで翌月の推奨注文量を予測できる
- 「最新情報を取得」ボタンから Meteostat API を呼び出し、気象データをリアルタイムで最新化できた
- モデル（Ridge + LOO-CV）を予測のたびに自動で再学習する設計にしたため、実績データが増えるたびに精度が自動更新される

### できなかったこと

| 項目 | 理由 |
|---|---|
| 翌月の実気温を使用した予測 | Meteostat APIでは気温の実績しか取得できないため、現状は前年同月の実績値で代替している |
| 前月繰越の残水量を考慮した予測 | remaining_quantity のデータが未収集のため、モデルの特徴量に含められていない |
| 湿度を特徴量に追加すること | Meteostat APIでは湿度データが取得できないため断念した |
| 月の途中時点での水量不足アラート | 月ごとの実績でしか管理していないため、日ごとの消費量計測に対応していない |

### 気づいたこと・学んだこと

- LOO-CVはデータ件数が少ない（月次10件程度）場合に特に有効で、k-分割CVよりも評価が安定した
- 「前年同月気温」の代替は、気候変動の影響を受けるため年によって誤差が出やすい。最終的には天気予報APIから翌月予測気温を取得することが最も改善効果が高い
- 特徴量が3つ（営業日数・平均出社人数・平均気温）と少ないため、モデルの挙動が直感的に解釈しやすかった。係数を確認することで「気温が高いほど消費が増える」という常識的な関係が数値で確認できた
- FastAPI でモデルを毎回再学習する構成にしたことで、データが増えるたびに自動的に精度が更新され、運用上の手間がほぼゼロになった

### 次回やってみたいこと・改善したいこと

- 前月の残水量をデータとして記録し、特徴量に加えることで予測精度のさらなる向上を図りたい
- 湿度データが取得できる気象APIへの移行（Open-Meteo など）を検討し、湿度を特徴量に追加したい
- 社員ごと日ごとの水の消費量予測ができるようにしたい

---

## STEP7 デモ実施

### フィードバック内容

| # | フィードバック内容 |
|---|---|
| ... | ... |

### 次回テーマへのアイデア

- ...
